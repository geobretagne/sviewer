/* Test profile — loaded by ?c=test in the test suite.
 * Values are fixed and known so suites can assert on them.
 * Do not change without updating tests/suites/02-config-merge.js.
 */
window.customConfig = {
    title: 'sViewer Test Profile',
    initialExtent: [-582000, 5977000, -104000, 6268000],
    maxFeatures: 7
};
